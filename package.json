{
  "name": "corinth",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:engine && vite build",
    "build:engine": "esbuild engine/index.ts --bundle --format=esm --outfile=engine.bundle.js",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "tsx engine/apply.test.ts",
    "dev:party": "npm run build:engine && npm run dev --prefix server",
    "dev:all": "npm run build:engine && npx concurrently -k \"npm run dev --prefix server\" \"vite\"",
    "dev:game": "vite",
    "dev:online": "npm run build:engine && concurrently -k -n party,web -c blue,green \"npm run dev --prefix server\" \"vite --port 5173\"",
    "build:pages": "VITE_BASE=/ npm run build",
    "deploy:pages": "npm run build:pages && wrangler pages deploy dist --project-name=corinth --branch=main --commit-dirty=true"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "esbuild": "^0.25.9",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "partysocket": "^1.3.0"
  }
}
